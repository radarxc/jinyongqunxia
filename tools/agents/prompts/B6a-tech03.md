# 本任务：续写 `docs/tech/03-mobile-performance.md`（§6.8 起至文末）

## 现状

文档写到 §6.7"着色器编译与 GPU 上传的时间切片"为止，撰写代理在那里被中断。以下部分已经写好，是续写的约束：
- 文首的"结论先行（TL;DR）"14 条；
- "基线核查结论"F1–F14；
- 目录；
- §0 关键决策 D1–D16；
- §1–§6.7 正文。

续写内容必须与它们一致。如果调研发现必须改变某个已写的决策，同时修改 TL;DR 与 §0 中对应的条目，并在报告中说明原因。

## 需要补写

- **§6 剩余小节**：按需判断，例如音频与视频解码的内存、字体加载时机、输入延迟等。已有 6.1–6.7。
- **§7 自适应质量**：静态探测 → GPU / 内存规则 → 标题画面活画基准 → 运行时（动态分辨率 0.05 步进、CPU / GPU 瓶颈判别、会话内只降不升、垂直同步封顶识别）→ 温控调速器 T0–T3。
- **§8 性能测试**：真机手工清单；HUD（`?perf=1`）与 `window.__tsPerf` 报告接口；CI（Playwright + CDP 的 6 个确定性基准场景，放在 `tools/perf/`）；门禁规则（计数型指标精确门禁、时间型指标 +10% 报警）；可选的自托管安卓真机夜跑。
- **§9 网络与离线**：离线范围；按内容寻址文件粒度的断点续传；视频分段；微信内置浏览器；弱网策略。
- **§10 性能风险 Top 10 与预案**；**§11 MVP 与演进路径（性能视角）**；**§12 备选方案**。
- **参考资料**：本文引用的全部来源，包括 TL;DR 与 F1–F14 中提到的；**本文新增术语/约定**；**待决事项 / 依赖**。

风格与深度与前文一致：表格、代码片段、明确的数值。续写前先通读 §1–§6，沿用其中定义的名称（如 `memClass`、`fpsMode`、`GpuBudget`、`RenderScheduler`）。
