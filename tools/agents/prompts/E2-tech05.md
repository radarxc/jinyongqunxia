# 本任务：撰写 `docs/tech/05-gameplay-engine.md`（玩法引擎）

职责：纯 TypeScript、无 DOM、确定性的玩法核心 `packages/core` 的内部设计——战斗与集气、行动与反应、Buff DSL 运行时、伤害管线实现、任务 DSL、存档状态、AI、确定性与回放。

## 必读

- `tech/01`：§3 总体架构、§6 核心运行时、§8.3 确定性规则 D1–D9 与禁用 API 清单、待决 P2 / P3 / P10。
- `tech/03`：Worker 模式与帧预算。`tech/04`：数据格式与书界包。`tech/08`：§3 存档容器与 `GameState`、战斗日志。
- `design/04`（公式与取整点）、`05`、`06`（§2 数据结构、§4 叠加、§5 时机、§6 触发器与效果原语）、`08`（地形与高度规则）、`09`（§2–§8、§13 数据结构与接口）、`12`（任务与 NPC 规则）、`13`（经验、难度规则开关）。

## 至少包含

1. 模块划分与依赖方向；状态模型（`GameState` 结构、可变与不可变的策略、快照）；命令 → 事件 → 状态的更新模型。
2. **确定性**：种子随机数（算法、流分离）、整数与万分点算术、禁用 API 与 lint 规则、跨引擎 golden 测试（Node + WebKit）。
3. **战斗引擎**：集气时间轴（design/09 §3）、行动经济、反应（招架、反击、连击）、合击、阵法、环境行动者、就地开战的网格生成。
4. **伤害管线**：design/04 的 Z0–Z10 与结算逐区实现、取整点、调试追踪（每次伤害可以输出乘区明细）。
5. **Buff DSL 运行时**：事件钩子（06 的 59 个）、效果原语（约 50 个）、叠加与品阶对抗 ρ(Δ)、性能（事件分发、热路径零分配）。
6. **任务 DSL** 与条件表达式（design/12）；世界状态与旗标；Ink 变量桥接（tech/04）。
7. **AI**：design/09 §8 行为模型的实现（效用 AI 或行为树，说明选择理由）、时间预算（tech/03）。
8. **存档与回放**：状态序列化、迁移接口（tech/08 §3.5）、战斗录像（开局快照 + 命令序列 + 终局哈希）与重放。
9. 探索逻辑 tick 的终值（tech/01 P3）与世界时间换算（与 design/11 对齐）。
10. 测试策略、性能预算、MVP 与演进、风险、参考资料、本文新增术语/约定、待决事项 / 依赖。

代码片段用 TypeScript（strict）。篇幅参考：1,300–2,000 行。
