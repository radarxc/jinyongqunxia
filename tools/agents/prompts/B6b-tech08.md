# 本任务：续写 `docs/tech/08-backend-and-online.md`（§4 起至文末）

## 现状

文档写到 §3.6"内容版本与 ID 重映射"为止，撰写代理在那里被中断。以下部分已经写好，是续写的约束：
- 文首表格；
- "结论先行（TL;DR）"12 条；
- "调研要点"12 条；
- 目录（§0–§15、参考资料、新增术语、待决事项）；
- §0–§3 正文。

续写内容必须与它们一致。如果调研发现必须改变某个已写的决策，同时修改 TL;DR 中对应的条目，并在报告中说明原因。

## 需要补写（按目录）

- **§4 同步与冲突**：整快照 + 服务端修订号 CAS（`If-Match`）；自动存档按设备分命名空间；具名槽冲突让玩家选择，落选版本进历史 30 天；离线队列在应用层实现（Background Sync 与 `keepalive` 的限制见调研要点 5）；iOS 主屏 Web App 与 Safari 存储隔离的迁移（tech/03 F14）。
- **§5 认证、会话与安全**：MVP 主配对密钥 + 临时配对码 → HttpOnly 会话 Cookie `ts_s`（30 天滚动）；Phase 3 以 Passkey 为主、邮箱验证码为恢复；不用魔法链接（原因见 TL;DR 5）；与 tech/06 素材闸门共用会话；威胁模型。
- **§6 API 设计**：REST 路由（与 §2.2 路由总表一致）、请求与响应、错误码、版本。
- **§7 数据模型（D1 + R2）**：表结构 DDL、R2 键规则、索引、限额（D1 单行 2 MB 等，见调研要点 2）。
- **§8 部署方案对比**：加权决策矩阵（方案 A Cloudflare 92 分等，见 TL;DR 6）、国内方案（ICP 备案）、成本。
- **§9 AI NPC 自由对话（可选，默认关闭）**：与 TL;DR 8–9 一致（模型、`effort`、流式、人设卡、提示缓存、规则校验、失败回退、地区合规开关、服务端回退）。
- **§10 战斗日志与数值遥测（可选）**；**§11 远程配置与版本通知**；**§12 运维**（备份、告警、导出）。
- **§13 服务端目录结构与关键代码**（Hono + 存储适配器，TypeScript）。
- **§14 MVP 与演进路径**；**§15 风险与备选方案**。
- **参考资料**（含 TL;DR 与调研要点中提到的全部来源）、**本文新增术语/约定**、**待决事项 / 依赖**。

风格与深度与前文一致：表格、TypeScript 片段、明确的数值与限额。
