# 本任务：审校 {{target_id}}（{{target_title}}）

上一名代理刚完成任务 {{target_id}}，产出：{{target_files}}。你是审校代理，要找出问题并**直接修正**，让产出达到可定稿的质量。它收到的原任务要求附在文末，作为验收标准。原任务的报告在 `tools/agents/reports/{{target_id}}.md`，先读它。

## 审校清单

1. **完整性**：原任务要求的章节、表格、数量下限是否全部满足；有无截断、占位、没写完的句子；目录与正文是否一致。缺的补写。
2. **基准一致性**：术语、ID、品阶、数值尺度、乘区名称、装配栏规则是否与 `docs/00-canon.md` 一致；是否越界重定义了其他文档的概念（改为引用）。
3. **数值**：抽查至少 10 处关键公式与数值并复算。错误直接改，在报告中列出"位置 / 原值 → 新值 / 依据"。
4. **考据**（策划文档）：检查原著事实。错误的改正，没把握的标（待考）。不要编造。
5. **事实核查**（技术文档）：版本号、API、浏览器支持、价格、限额。能联网就逐条核实并补来源；不能核实的保留（待核实）。
6. **跨文档一致性**：与 `docs/decisions/rulings-v1.md` 的裁定、相关文档的接口与数据结构是否一致。若 `tools/lint/check_ids.py` 已存在，运行它，处理与本文有关的问题。
7. **可执行性**：YAML、TypeScript、Python 片段语法正确，字段与归属文档的数据结构一致；脚本能运行。
{{review_focus}}

## 修改原则

- 修正错误、补齐缺漏，保留合理内容。不要为改而改，也不要大段删减：调度器会拒绝把文件缩短 15% 以上的结果。
- 若文档头部有版本信息，追加"审校 {{task_id}}（{{date}}）"。
- 报告第 7 节改为"审校结论"：通过 / 有条件通过 / 不通过；按严重度（阻断 / 重要 / 一般）列出发现的问题和处理方式；再列出没能处理、需要后续任务跟进的问题。

---

## 附：原任务要求

{{target_prompt}}
